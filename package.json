{
	"devDependencies": {
		"postcss-import": "^16.1.0",
		"postcss-nesting": "^12.1.1",
		"tailwindcss": "^3.4.3"
	},
	"scripts": {
		"build": "npx tailwindcss build -i index.css -o dist/styles.css -m",
		"dev": "npx tailwindcss build -i index.css -o dist/styles.css --watch -m",
		"dev:membership": "npx tailwindcss build -i membership.css -o dist/membership.css --watch -m"
	}
}
